    ; create pointer at zp $10 -> points to $0030
    lda #$20
    sta $0010
    lda #$03
    sta $0011
    ldy #$05
    lda ($10),y
    jmp done

    .org $0030
    .byte $55

    .org $00f0
done:
    jmp done

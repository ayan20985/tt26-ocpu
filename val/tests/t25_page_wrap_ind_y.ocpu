    ; ind,y: zero-page pointer at $10/$11 points to $0200, Y=0, read stored value
    lda #$20
    sta $0200
    lda #$00
    sta $0010
    lda #$02
    sta $0011
    ldy #$00
    lda ($10),y
    jmp done

    .org $0100
done:
    jmp done
